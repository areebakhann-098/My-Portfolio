<mat-sidenav-container class="h-screen">
  <mat-sidenav mode="side" opened class="w-64 bg-white shadow-lg">
    <div class="p-6">
      <!-- User Info -->
      <div class="flex items-center gap-3 mb-10">
        <img [src]="userLogo" alt="Logo" class="rounded-full w-10 h-10" />
        <h2 class="text-lg font-bold">{{ userName }}</h2>
      </div>

      <!-- Navigation -->
      <mat-nav-list>
        <a
          mat-list-item
          *ngFor="let link of links"
          [routerLink]="link.path"
          routerLinkActive="text-blue-600"
        >
          <mat-icon class="mr-4">{{ link.icon }}</mat-icon>
          <span>{{ link.label }}</span>
        </a>
      </mat-nav-list>
    </div>
  </mat-sidenav>
  
  <!-- Main Content -->
  <mat-sidenav-content class="p-6">
    <router-outlet></router-outlet>
  </mat-sidenav-content>
</mat-sidenav-container>
